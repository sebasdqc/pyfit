django>=5.2,<6.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.4,<5.0
django-unfold>=0.40,<1.0
django-otp>=1.5,<2.0
qrcode>=7.4,<9.0
django-auditlog>=3.0,<4.0
sentry-sdk[django]>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
groq>=0.9,<2.0                  # (legacy) aún referenciado por algunos tests; el runtime usa DeepSeek vía openai
openai>=1.30,<2.0               # cliente LLM (DeepSeek, OpenAI-compatible) — ver pyfit/llm.py
python-dotenv>=1.0,<2.0
dj-database-url>=2.0,<3.0
gunicorn>=22.0,<24.0
whitenoise>=6.0,<7.0
# Google Sign-In — verificación del id_token enviado por la app
google-auth>=2.30,<3.0
# Apple Sign-In — verificación del identity_token (JWT) contra el JWKS público de Apple
PyJWT>=2.8,<3.0
# Garmin integration
cryptography>=42.0,<44.0        # Fernet para encriptar tokens OAuth (EncryptedTextField propio)
requests>=2.32,<3.0             # llamadas HTTP a Wellness API de Garmin
# Celery (sync diario Garmin)
celery[redis]>=5.4,<6.0         # task queue + Redis broker
django-celery-beat>=2.6,<3.0    # almacena el beat schedule en Django DB
# Validación de imágenes — reemplaza imghdr (eliminado en Python 3.13)
Pillow>=10.0,<12.0
# Object storage (DO Spaces / S3) — fotos de atletas del panel Performance
django-storages>=1.14,<2.0
boto3>=1.34,<2.0
# Tutor IA de Academy — embeddings LOCALES para el RAG (grounding sobre los cursos).
# OJO: arrastra torch (pesado). El Dockerfile instala torch CPU-only para no bajar
# los wheels CUDA (~GB) y pre-baja el modelo. Si el build de DO se complicara, se
# puede quitar esta línea: el tutor degrada a recuperación por palabras
# (ver ai_tutor/retrieval.py) hasta reactivarlo o migrar a embeddings por API.
sentence-transformers>=3.0,<6.0
